package DSAPrinceton;

public class PercolationExample {

    int[] grid;
    // create n-by-n grid, with all sites blocked
    public PercolationExample(int n) {
        grid = new int[n];
    }
    // open site (row, col) if it is not open already
    public void open(int number) {
        
    }
    
}
